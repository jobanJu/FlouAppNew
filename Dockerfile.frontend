# Frontend Dockerfile (Expo)
FROM node:22-alpine

WORKDIR /app

# Copy frontend code and dependencies
COPY package*.json .
RUN npm ci

COPY . .

# Expose Expo port
EXPOSE 8081

# Start Expo dev server (for production use)
CMD ["npm", "start"]
